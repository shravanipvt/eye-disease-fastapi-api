# Eye Disease Detection API (FastAPI + PyTorch)

A FastAPI backend that classifies eye disease images using an EfficientNet-B4 PyTorch model.

## 🚀 Features
- FastAPI backend
- PyTorch model
- Auto-download model weights
- Docker-ready
- Free deployment supported on Render / Railway

---

## 📂 Project Structure

